import { Component } from '@angular/core';

@Component({
  selector: 'app-home', // Selector del componente, se utilizará como una etiqueta HTML <app-home></app-home>
  templateUrl: './home.component.html', // Ruta del archivo de plantilla HTML asociado al componente
  styleUrls: ['./home.component.css'] // Rutas de los archivos de estilos CSS asociados al componente
})
export class HomeComponent {
  // Clase del componente HomeComponent que define el comportamiento y las propiedades del componente
}
