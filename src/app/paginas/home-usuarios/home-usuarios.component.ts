import { Component } from '@angular/core';

// Componente HomeUsuariosComponent
@Component({
  selector: 'app-home-usuarios', // Selector del componente
  templateUrl: './home-usuarios.component.html', // Ruta de la plantilla del componente
  styleUrls: ['./home-usuarios.component.css'] // Rutas de los archivos de estilo del componente
})
export class HomeUsuariosComponent {
  // Clase del componente HomeUsuariosComponent
}
