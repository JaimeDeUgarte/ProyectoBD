import { Component } from '@angular/core'; // Importar la clase Component del paquete '@angular/core'

@Component({
  selector: 'app-hadmin', // Selector del componente, se utiliza como etiqueta HTML para utilizar el componente en las plantillas
  templateUrl: './hadmin.component.html', // Ruta al archivo de plantilla HTML del componente
  styleUrls: ['./hadmin.component.css'] // Rutas a los archivos de estilos CSS del componente
})
export class HAdminComponent { // Declarar la clase del componente HAdminComponent

}
